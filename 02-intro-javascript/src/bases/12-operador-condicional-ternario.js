console.log("Hola Nundo");

// operador condicional ternario
// es una manera corta de hacer una definición

const activo = false;

// let mensaje;

// if (activo) {
//   mensaje = "Activo";
// } else {
//   mensaje = "Inactivo";
// }

const mensaje = activo ? "Activo" : "Inactivo";

// solo ejecutar el if
const mensaje2 = activo && "Activo";

console.log(mensaje);

console.log(mensaje2);
